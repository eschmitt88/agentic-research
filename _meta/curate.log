Done. Promoted the **evaluation** cluster into a new Map of Content.

## What I did

**Detected** one ripe cluster among the 21 concepts:
- **evaluation (5 concepts)** — `hce-evaluation`, `pass-at-k`, `citation-anchoring`, `typed-claim-partition`, `programmable-evaluator-oracle`. The existing `knowledge-organization` MoC covers only 2 of the 5 (below majority), so this was un-mapped and ripe.

**Declined** (correctly, with restraint):
- **memory (4)** — already majority-mapped in knowledge-organization; confirms the prior NOTES finding that it's not ready for its own MoC.
- **evolutionary-search (3)** — too few concepts.

**Synthesized** `mocs/evaluation-integrity.md` from the actual concept files, organized as a containment hierarchy:
1. *Keeping the search loop from gaming itself* — HCE split + pass@k seed distributions
2. *Grounding the claims* — citation-anchoring → typed-claim-partition (flag → control signal)
3. *The oracle that defines what's discoverable* — programmable-evaluator-oracle

Framing centers on AIRA_2's named bottleneck (validation-selection overfitting), with anchored literature citations throughout, and an open-thread hypothesis (the concepts compose multiplicatively) plus the most testable interaction for this repo.

**Wired in:** added to `_meta/index.md`, logged to `_meta/log.md`. Skipped concept-side backlinks since none of the five carry a `mocs:` field (no schema invention). Committed and pushed (`8b24a2d`) so the Pages site updates.
Promoted a new Map of Content. Pushed as `3ca28c4`.

## What I found

22 concepts, 15 already mapped across 3 MoCs. Surveying the 7 unmapped ones by theme surfaced **one ripe, coherent cluster**:

**`mocs/autonomous-search-loop.md`** — *The Autonomous Experiment-Search Loop* (5 concepts)
- `evolutionary-expansion` + `evolutionary-search-grain` — the search strategy and its mutation unit
- `programmable-evaluator-oracle` — the fitness oracle that defines what's discoverable
- `async-worker-pool` + `budget-as-ceiling` — the operational disciplines (parallelism, halting) that make a campaign runnable

All five converge on the FunSearch → AlphaEvolve → FM Agent lineage plus AIRA_2's bottleneck framing. Overlap with existing MoCs is only `programmable-evaluator-oracle` (1/5 = 20% < 50% threshold) — it's a legitimate dual-membership seam with `evaluation-integrity`, which I flagged in both directions, mirroring how that MoC already shares `citation-anchoring`/`typed-claim-partition` with knowledge-organization.

## Clusters I declined

- **Memory** (`agent-native-memory`, `context-eviction-policy`, `multi-granularity-memory`, `selective-memory-retrieval`) — only 4, and 3 already live in `knowledge-organization`. Same conclusion the prior NOTES reached; enrich-existing remains correct.
- **Skills** (`shared-skill-namespace`, `skill-library-lifecycle`) — 2, far short.
- **Safety/runtime-governance** (`permission-gate-as-architecture`, +stragglers) — no coherent ≥5 set.

Skipped the frontmatter backlink step: concepts here carry no `mocs:` field, so per the skill I didn't invent one (consistent with the existing MoCs). Index and `_meta/log.md` updated.
Pushed cleanly (`290a9c8..3063f25`) — the prior auto-push failure didn't recur.

## Summary

**No new MoC was ripe** — every ≥5-concept cluster is already substantially covered by one of the 4 existing MoCs. Per the skill's restraint clause, I declined to manufacture a redundant one and instead took the correct enrich action.

**Declined (reported, not created):**
- Standalone **memory** MoC — 4 of its 5 concepts already live in knowledge-organization
- **Safety/governance** MoC — only 2 concepts (`permission-gate-as-architecture` + `budget-as-ceiling`)
- **Skills-interop** MoC — only 2 concepts (`shared-skill-namespace` + `skill-library-lifecycle`)

**Enriched** `mocs/knowledge-organization-for-research-agents.md` (9 → 10 concepts): added `context-eviction-policy` — the lone genuinely-unmapped memory concept, which frames itself as the fourth memory axis and was already referenced inline in the MoC. Placed it in the read-side section as the eviction dual of `selective-memory-retrieval`. Updated `_meta/index.md` and `_meta/log.md`. Committed and pushed (`3063f25`).

This mirrors last session's `multi-granularity-memory` enrichment — same restraint, same correct home. The two remaining unmapped concepts (`permission-gate-as-architecture`, `shared-skill-namespace`) stay un-promoted until their themes accumulate ≥5 concepts.
I have a complete picture. Let me run the ripeness analysis.

## Ripeness analysis

**22 concepts, 4 existing MoCs.** MoC coverage of every concept:

| MoC | Concepts | Count |
|-----|----------|-------|
| agent-architecture | agent-native-memory, file-as-bus, hierarchical-delegation, hybrid-model-backends, scripted-tool-pipelines | 5 |
| autonomous-search-loop | evolutionary-expansion, evolutionary-search-grain, programmable-evaluator-oracle, async-worker-pool, budget-as-ceiling | 5 |
| evaluation-integrity | hce-evaluation, pass-at-k, citation-anchoring, typed-claim-partition, programmable-evaluator-oracle | 5 |
| knowledge-organization | agent-native-memory, file-as-bus, structured-world-model, skill-library-lifecycle, typed-claim-partition, citation-anchoring, selective-memory-retrieval, context-eviction-policy, multi-granularity-memory, web-grounded-literature | 10 |

**Only 2 concepts are unmapped:** `permission-gate-as-architecture` and `shared-skill-namespace`. They share no coherent theme (safety/governance vs. skill interop), so they don't form a cluster.

**Candidate clusters evaluated and declined:**

- **agent-memory** — `{agent-native-memory, selective-memory-retrieval, context-eviction-policy, multi-granularity-memory, skill-library-lifecycle}`. This *is* ~5 concepts, but **all five are already in the knowledge-organization MoC** (5/5 mapped ≫ the "≥ half" threshold). A new memory MoC would be pure duplication. Prior NOTES (06-24, 06-25) reached the same verdict twice; enrich-existing was already done (multi-granularity-memory was added to knowledge-organization, 8→9→10). **Declined — redundant.**
- **safety / runtime-governance** — `{permission-gate-as-architecture, budget-as-ceiling}` (+ loose ties to programmable-evaluator-oracle, scripted-tool-pipelines, which are mapped elsewhere). Only 2 genuinely safety-themed concepts. **Declined — below 5, prior NOTES declined a safety MoC for the same reason.**
- **skills / harness-interop** — `{shared-skill-namespace, skill-library-lifecycle}`. Only 2, and skill-library-lifecycle is already mapped. **Declined — below 5.**
- **evolutionary-search** — fully covered by autonomous-search-loop. **Mapped.**

## Verdict: nothing is ripe — no MoC created

No un-mapped cluster reaches ≥5 related concepts. The single ripe-sized cluster (agent-memory) is already substantially covered by the knowledge-organization MoC, so promoting it would duplicate rather than add structure. The two orphan concepts don't cohere into a theme.

Per the skill's restraint clause, I'm stopping rather than manufacturing a thin or redundant MoC. No files changed, nothing to commit.

The graph's structural pressure point is elsewhere: **knowledge-organization is now at 10 concepts** with a clear memory sub-spine inside it. That's the one to watch — if 1-2 more *un-mapped* memory concepts land (not already in knowledge-organization), carving a dedicated `agent-memory` MoC out of the parent would become the correct split. It isn't yet.
## /promote-moc — detection result: nothing is ripe

I surveyed all 22 concepts (excluding `_README`), grouped them by shared `tags:`, and checked overlap against the 4 existing MoCs. **No un-mapped cluster clears the ripeness bar**, so I'm declining to promote — per the skill's restraint rule, a thin or redundant MoC stays un-built.

### The map as it stands

| MoC | concepts |
|---|---|
| `agent-architecture` | agent-native-memory, file-as-bus, hierarchical-delegation, hybrid-model-backends, scripted-tool-pipelines |
| `autonomous-search-loop` | evolutionary-expansion, evolutionary-search-grain, programmable-evaluator-oracle, async-worker-pool, budget-as-ceiling |
| `evaluation-integrity` | hce-evaluation, pass-at-k, citation-anchoring, typed-claim-partition, programmable-evaluator-oracle |
| `knowledge-organization-for-research-agents` | agent-native-memory, file-as-bus, structured-world-model, skill-library-lifecycle, typed-claim-partition, citation-anchoring, selective-memory-retrieval, context-eviction-policy, multi-granularity-memory, web-grounded-literature |

20 of 22 concepts are mapped. Only **2 are un-mapped**: `permission-gate-as-architecture` and `shared-skill-namespace`.

### Candidate clusters considered and declined

- **Memory** (`agent-native-memory`, `context-eviction-policy`, `multi-granularity-memory`, `selective-memory-retrieval`, +`skill-library-lifecycle`'s procedural-memory) — 4–5 concepts, but **all already live in `knowledge-organization`**. A memory MoC would be ~100% redundant. (Same conclusion the last two sessions reached; still holds.)
- **Safety / governance** (`permission-gate-as-architecture` + `budget-as-ceiling`, maybe `hce-evaluation`) — only 1 of these is un-mapped; the others are anchored in `autonomous-search-loop`/`evaluation-integrity`. Pulling them into a new MoC would create overlap across three existing maps for a 2–3 concept theme. Not ripe (also declined previously).
- **Runtime policy** (`context-eviction-policy`/runtime-policy, `selective-memory-retrieval`/runtime-decisions, `permission-gate-as-architecture`/runtime-approval) — a genuinely interesting cross-cut, but only **3 concepts**. Below threshold.
- **The 2 un-mapped concepts** don't share a theme with each other (runtime governance vs. skill-format interoperability), so they can't seed a cluster.

### Watch-list (not yet ripe, closest to becoming so)

The **runtime-policy / runtime-governance** cross-cut is the most promising future MoC: it would need ~2 more concepts on runtime decision-gating (beyond eviction, retrieval-gating, and permission-gating) to clear 5 and, crucially, it would be a *non-redundant* organizing axis — these concepts currently sit scattered across three different MoCs. If the next digest batch adds runtime-decision concepts, re-run `/promote-moc runtime-governance`.

No files written, no commit — correctly a zero-promotion cycle.
I now have the full coverage picture. Let me analyze the clusters against the ripeness test.

**Coverage:** 20 of 22 concepts are already mapped into the four existing MoCs. Only two are unmapped: `permission-gate-as-architecture` and `shared-skill-namespace` — and they share no common theme.

Let me check the candidate clusters by shared tag:

| Theme | Concepts | Count | Already mapped? |
|---|---|---|---|
| **memory** | agent-native-memory, context-eviction-policy, multi-granularity-memory, selective-memory-retrieval | 4 | All 4 in knowledge-organization MoC |
| **evaluation** | hce-evaluation, pass-at-k, citation-anchoring, typed-claim-partition, programmable-evaluator-oracle | 5 | All 5 in evaluation-integrity MoC |
| **agent-architecture** | agent-native-memory, file-as-bus, hierarchical-delegation, hybrid-model-backends, scripted-tool-pipelines | 5 | All 5 in agent-architecture MoC |
| **knowledge-organization** | multi-granularity-memory, selective-memory-retrieval, skill-library-lifecycle, typed-claim-partition | 4 | All in knowledge-organization MoC |
| **evolutionary-search** | evolutionary-expansion, evolutionary-search-grain, programmable-evaluator-oracle | 3 | All in autonomous-search-loop MoC |
| **skills / governance** (unmapped) | shared-skill-namespace, permission-gate-as-architecture | 2 | Unmapped, but no shared theme |

## Verdict: nothing is ripe — declining to promote

No cluster satisfies **both** ripeness conditions (≥5 related concepts **and** not already mapped):

- **Memory** (theme prior NOTES flagged) — still only **4** memory-tagged concepts, and all four already live in the knowledge-organization MoC. Below the threshold *and* fully covered. Same finding as the 2026-06-26 session; nothing has changed it.
- **Evaluation, agent-architecture, evolutionary-search** — each is a real ≥5 (or near) cluster, but each is *already its own MoC*. A new MoC would duplicate, not add structure.
- **The two unmapped concepts** (`shared-skill-namespace`, `permission-gate-as-architecture`) don't form a coherent cluster — one is harness-interop, the other is runtime-safety/governance — and there are only two of them. The prior session already declined a safety MoC on the same grounds; nothing new has accumulated since.

Per the skill's restraint clause and `agency.md` ("declines redundant ones — keep that restraint; do not manufacture thin MoCs"), I'm **not** creating or modifying any MoC. No files changed, nothing to commit.

**Watch list for next time** — the closest-to-ripe theme is **agent-memory**: one more memory concept (a 5th) that isn't already absorbed by knowledge-organization would tip it. If the next ingest batch adds a memory concept with a distinct enough angle, a dedicated memory MoC (splitting from knowledge-organization) becomes worth reconsidering.
I have enough to judge ripeness. Let me lay out the cluster analysis.

**Mapping status (21 real concepts, `_README` excluded):**

Four MoCs already exist. Cross-referencing every concept against them, only **two concepts are unmapped**:
- `permission-gate-as-architecture` (safety / governance / runtime-approval)
- `shared-skill-namespace` (skills / interop / standardization)

**Candidate clusters, against the ripeness test (≥5 related AND not already majority-mapped):**

| Theme | Concepts | Count | Verdict |
|---|---|---|---|
| Evaluation | citation-anchoring, hce-evaluation, pass-at-k, programmable-evaluator-oracle, typed-claim-partition | 5 | **Mapped** — `evaluation-integrity` covers all 5 |
| Agent architecture | agent-native-memory, file-as-bus, hierarchical-delegation, hybrid-model-backends, scripted-tool-pipelines | 5 | **Mapped** — `agent-architecture` covers all 5 |
| Knowledge-organization | multi-granularity-memory, selective-memory-retrieval, skill-library-lifecycle, typed-claim-partition (+6 more) | 10 | **Mapped** — `knowledge-organization-for-research-agents` |
| Memory | agent-native-memory, context-eviction-policy, multi-granularity-memory, selective-memory-retrieval | 4 | **Not ripe** — only 4; half already in knowledge-org MoC (unchanged since last session) |
| Safety / runtime-governance | permission-gate-as-architecture, budget-as-ceiling (+ runtime-policy stragglers) | 2 strong | **Not ripe** — only 2 genuine; the rest are memory-runtime concepts already mapped elsewhere |
| Skills / interop | shared-skill-namespace, skill-library-lifecycle, scripted-tool-pipelines | 2–3 | **Not ripe** — too few, and 2 already mapped |

## Verdict: nothing is ripe — no MoC promoted.

No un-mapped theme reaches 5 concepts. The two genuinely unmapped concepts (`permission-gate-as-architecture`, `shared-skill-namespace`) sit on **different** themes and don't combine into a coherent ≥5 cluster.

**Clusters I explicitly declined, and why:**
- **Memory** — still 4 memory-tagged concepts (no change since 2026-06-28); `agent-native-memory` and `selective-memory-retrieval` already live in `knowledge-organization`. Enrich-existing remains correct; a standalone memory MoC would duplicate.
- **Safety / runtime-governance** — tempting given `permission-gate` is now a 4th-domain anchor, but only 2 concepts (`permission-gate`, `budget-as-ceiling`) truly fit. Pulling in `context-eviction-policy` / `selective-memory-retrieval` to pad to 5 would be exactly the thin/redundant manufacture the rule forbids — those are memory-runtime concepts already mapped.
- **Skills / interop** — `shared-skill-namespace` is a fresh seedling but has only `skill-library-lifecycle` and a marginal `scripted-tool-pipelines` nearby; too thin.

**Watch-list (no action now):** `permission-gate-as-architecture` is the seed of a potential *safety / runtime-governance* MoC. If one or two more governance/safety concepts get ingested (e.g. a sandbox-policy or autonomy-bound note), that cluster could cross the threshold — worth re-checking after the next digest/curate burst.

Nothing written, no commit — consistent with the rule that restraint is the point. The graph is well-mapped: 19 of 21 concepts are in a MoC, and the two that aren't don't yet form a cluster.
Let me build the full mapped/unmapped picture.

I have everything I need. Here's the analysis.

## `/promote-moc` — auto-detect: nothing ripe, no MoC created

**Coverage map.** 22 concepts (excluding `_README`), 4 existing MoCs. Only **2 concepts are unmapped**:

- `permission-gate-as-architecture` (tags: safety, permission-gate, runtime-approval, regulator, tool-use, governance)
- `shared-skill-namespace` (tags: skills, interop, format-portability, harness-ecosystem, standardization)

Two orphans on unrelated themes can't form a ≥5-concept cluster. So the only way to reach ripeness is via a *thematic* cluster spanning already-mapped concepts — and each candidate fails the "not already mapped" test:

**Candidate clusters considered, all declined:**

| Cluster | Members | Why declined |
|---|---|---|
| **Memory / retrieval** | agent-native-memory, context-eviction-policy, multi-granularity-memory, selective-memory-retrieval, skill-library-lifecycle, web-grounded-literature (6) | Ripe by count, but **knowledge-organization MoC already covers all 6**. Majority-mapped → not ripe. (Same verdict as the 2026-06-29 NOTES; enriching knowledge-org was the right move, and it's even more covered now.) |
| **Safety / governance / autonomy-control** | permission-gate-as-architecture, budget-as-ceiling (+ maybe hierarchical-delegation) | Only ~2 coherent members; `budget-as-ceiling` already in autonomous-search-loop. Below the 5-concept floor. |
| **Skills / interop** | shared-skill-namespace, skill-library-lifecycle | 2 concepts. Below floor; `skill-library-lifecycle` already in knowledge-org. |
| **Tool-use / efficiency** | scripted-tool-pipelines, permission-gate-as-architecture | 2 concepts. Below floor. |

**Verdict: no cluster is ripe.** Every ≥5 cluster is already substantially mapped, and the genuinely un-mapped concepts (2) are too few and thematically unrelated to anchor a new MoC. Per the skill's restraint clause, I'm **not** manufacturing a thin or redundant MoC.

No files changed, so nothing to commit.

**Watch item for a future run:** `permission-gate-as-architecture` (seedling, a 4-domain-attested safety/governance idea) and `shared-skill-namespace` are the two orphans. If 3+ more safety/governance/tool-governance concepts accumulate (e.g. a sandboxing, audit-trail, or capability-scoping note lands), a **safety-and-governance** MoC becomes the likely first genuinely-new map. Until then, nothing to do.
I have the full picture. Let me map coverage.

**Concept → MoC coverage** (22 concepts, 4 existing MoCs):

| MoC | concepts |
|---|---|
| agent-architecture | agent-native-memory, file-as-bus, hierarchical-delegation, hybrid-model-backends, scripted-tool-pipelines |
| autonomous-search-loop | evolutionary-expansion, evolutionary-search-grain, programmable-evaluator-oracle, async-worker-pool, budget-as-ceiling |
| evaluation-integrity | hce-evaluation, pass-at-k, citation-anchoring, typed-claim-partition, programmable-evaluator-oracle |
| knowledge-organization | agent-native-memory, file-as-bus, structured-world-model, skill-library-lifecycle, typed-claim-partition, citation-anchoring, selective-memory-retrieval, context-eviction-policy, multi-granularity-memory, web-grounded-literature |

**Only 2 concepts are unmapped:** `permission-gate-as-architecture` and `shared-skill-namespace`.

## Verdict: nothing is ripe — no MoC created

Testing every candidate cluster against the two-part ripeness bar (≥5 concepts **and** not already majority-covered):

- **Memory** (`agent-native-memory`, `context-eviction-policy`, `multi-granularity-memory`, `selective-memory-retrieval`, ± `skill-library-lifecycle`/procedural-memory) — **declined.** Only 4 core memory-tagged concepts, and *all* of them already live in `knowledge-organization`. A memory MoC would duplicate, not organize. (Same conclusion the last session reached; nothing has changed since — these were the last seeds and were already routed into knowledge-organization.)
- **Safety / governance** (`permission-gate-as-architecture`) — **declined.** A single concept, no cluster. 1 ≠ 5.
- **Skills / interop** (`shared-skill-namespace`, `skill-library-lifecycle`) — **declined.** Only 2 concepts, and one is already in knowledge-organization. Not a cluster.
- **Coordination / long-horizon state** (`file-as-bus`, `structured-world-model`, `hierarchical-delegation`, `async-worker-pool`) — **declined.** 4 concepts, and every one is already mapped across agent-architecture / autonomous-search-loop / knowledge-organization. A new MoC here would substantially overlap three existing ones — the anti-pattern the skill explicitly warns against.

The graph is well-covered: 20 of 22 concepts sit in a MoC, and the 2 stragglers don't share a coherent ≥5 theme. Per the skill's restraint clause, I'm **not** manufacturing a thin or redundant MoC to have something to do.

No files changed, nothing to commit. The next real promotion opportunity will likely be a **skills/interop** MoC — once `shared-skill-namespace` and `skill-library-lifecycle` are joined by ~3 more skill-lifecycle/portability concepts from the digest backlog, that becomes a genuine un-mapped cluster worth mapping.
